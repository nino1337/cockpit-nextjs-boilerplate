{
  "name": "cockpit-cms",
  "version": "0.11.2",
  "license": "MIT",
  "scripts": {
    "start": "docker start cockpit",
    "docker:init": "docker run -d --name cockpit -p 8080:80 -v ${PWD}/cockpitData:/var/www/html/cockpitData agentejo/cockpit",
    "docker:login": "docker exec -it cockpit /bin/bash"
  }
}
