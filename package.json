{
  "name": "personal-webpage",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "init": "concurrently 'npm start --prefix ./cockpit' 'npm install --prefix ./next-app'",
    "start": "concurrently 'npm start --prefix ./cockpit' 'npm run dev --prefix ./next-app'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
